jQuery(document).ready(function($) {
//	if ($('[value=full]').length > 0)
//	{
//		$('[value=full]').click(function(ev) {
//			$('[value=full]').attr("checked", false);
//			alert("Za miesiąc z tego miejsca będziesz mógł pobrać plik pdf.");
//		});
//	}
//	
//	if ($('[value=pages]').length > 0)
//	{
//		$('[value=pages]').click(function(ev) {
//			$('[value=pages]').attr("checked", false);
//			alert("Za miesiąc z tego miejsca będziesz mógł pobrać plik pdf.");
//		});
//	}
//	
  /*  if ($('.data[name=dataOd]').length > 0)
    {
        $('.data[name=dataOd]').focusout( function(event) {
            tar = $(event.target);
            if (tar.val().length != 4)
            {
                tar = $(event.target).val(tar = $(event.target).val().substring(0,4));
                alert('wartość pola data musi mieć 4 znaki');
            }
        });
    }
    
    if ($('.data[name=dataDo]').length > 0)
    {
        $('.data[name=dataDo]').focusout( function(event) {
            tar = $(event.target);
            if (tar.val().length != 4)
            {
                tar = $(event.target).val(tar = $(event.target).val().substring(0,4));
                alert('wartość pola data musi mieć 4 znaki');
            }
        });
        
        $('button').clicked( function() {
            if ($('.data[name=dataDo]').val().length != 4 || $('.data[name=dataOd]').val().length != 4)
            {
                alert("wartości pola daty muszą mieć 4 znaki");
                return false;
            }
        });
    }
*/
});